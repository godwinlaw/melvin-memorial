.git
.git/**
.wrangler
.wrangler/**
.claude
.claude/**
node_modules
node_modules/**
.DS_Store
.env*
.dev.vars*
wrangler.jsonc
wrangler.toml
.gitignore
.gitkeep
**/.gitkeep
CLAUDE.md
README.md

# Internal docs (specs, plans) — never serve publicly
docs
docs/**

# Worker source is bundled into the Worker itself; don't serve it as a static asset
src
src/**

# DB migrations are applied via wrangler, not served
migrations
migrations/**

# Playwright + ad-hoc debug artifacts
.playwright-mcp
.playwright-mcp/**
test-results
test-results/**
*-debug.png
carousel.png

# Large video source files — hosted on YouTube, never deployed (Cloudflare caps assets at ~25MB)
assets/*.mov
